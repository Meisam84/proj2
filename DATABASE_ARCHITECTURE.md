# معماری دیتابیس سامانهٔ مدیریت برنامه‌های رادیویی و تلویزیونی

## خلاصهٔ کلی
این پروژه یک سامانهٔ جامع برای مدیریت و کنداکتور خودکار برنامه‌های رادیویی و تلویزیونی است.

---

## بخش 1: اطلاعات پایه و مرجع (جداول پایه)

### 1.1 Channel (شبکه‌ها و ایستگاه‌ها)
- نام شبکه
- کد شبکه (یکتا)
- نوع پخش (رادیو/تلویزیون)
- توضیحات
- وضعیت فعال/غیرفعال

**کاربرد**: تعریف تمامی شبکه‌ها و ایستگاه‌های پخش

---

### 1.2 Genre (ژانرها)
- نام ژانر
- کد ژانر (یکتا)
- توضیحات

**کاربرد**: دسته‌بندی برنامه‌ها به ژانرهای مختلف

---

### 1.3 Role (نقش‌ها)
- نام نقش (مثل: تهیه‌کننده، مجری، کارگردان)
- کد نقش (یکتا)
- توضیحات

**کاربرد**: تعریف انواع نقش‌های افراد در برنامه

---

### 1.4 Person (افراد)
- نام کامل
- ایمیل
- تلفن
- بیوگرافی
- وضعیت فعال/غیرفعال
- تاریخ ثبت

**کاربرد**: ذخیرهٔ اطلاعات تمامی افراد (تهیه‌کنندگان، مجریان، کارگردانان، نویسندگان، ...)

---

### 1.5 TargetAudience (مخاطب هدف)
- نام دسته‌بندی
- حداقل سن
- حداکثر سن
- توضیحات

**کاربرد**: تعریف مخاطبین هدف براساس گروه سنی

---

## بخش 2: اطلاعات برنامه‌ها (ورود اولیه)

### 2.1 Program (برنامهٔ اصلی)
- عنوان
- زیرعنوان
- توضیحات
- شبکه (ارتباط با Channel)
- ژانرها (ManyToMany)
- مخاطبین هدف (ManyToMany)
- مدت زمان معمولی (دقیقه)
- آیا برنامهٔ زنده است
- آیا تکرارپذیر است
- وضعیت (فعال/غیرفعال)
- زبان
- کلمات کلیدی

**کاربرد**: ثبت اطلاعات اساسی و ثابت هر برنامه

**نکتهٔ مهم**: هر برنامه می‌تواند چند بار پخش شود، لذا نمونه‌های مختلفی از آن ایجاد خواهد شد.

---

### 2.2 ProgramMember (اعضای برنامه)
- برنامه (ForeignKey → Program)
- فرد (ForeignKey → Person)
- نقش (ForeignKey → Role)
- آیا نقش اصلی است

**کاربرد**: اختصاص افراد مختلف به هر برنامه با نقش‌های مختلف

**مثال**:
- برنامهٔ «صفحهٔ اول» → احمد (تهیه‌کننده) + علی (مجری) + ...

---

## بخش 3: نمونه‌های برنامه (ورود برای هر پخش)

### 3.1 ProgramInstance (نمونهٔ برنامه)
- برنامه (ForeignKey → Program)
- شماره قسمت
- عنوان قسمت
- توضیحات قسمت
- زمان شروع برنامه‌ریزی‌شده
- زمان پایان برنامه‌ریزی‌شده
- زمان شروع واقعی
- زمان پایان واقعی
- وضعیت (مسودهٔ/در انتظار تایید/تایید‌شده/زمان‌بندی‌شده/در حال پخش/تکمیل‌شده/لغو‌شده)
- یادداشت‌ها

**کاربرد**: ایجاد هر نمونهٔ پخش برنامه (برای هر روز و هر ساعت)

**مثال**:
- برنامهٔ «صفحهٔ اول» در «۱۴۰۳/۹/۲۳ ساعت ۲۰:۰۰» = یک ProgramInstance
- همان برنامه در «۱۴۰۳/۹/۲۴ ساعت ۲۰:۰۰» = ProgramInstance دیگر

---

### 3.2 ProgramGuest (مهمانان برنامه)
- نمونهٔ برنامه (ForeignKey → ProgramInstance)
- فرد (ForeignKey → Person)
- ترتیب ظهور

**کاربرد**: ثبت مهمانان خاص برای هر نمونهٔ برنامه

---

### 3.3 ProgramAsset (منابع و فایل‌ها)
- نمونهٔ برنامه (ForeignKey → ProgramInstance)
- نوع منبع (اپنینگ/انگلیزی/فایل صوتی/فایل تصویری/زیرنویس/تصویر/سند)
- فایل
- عنوان
- توضیحات
- مدت زمان (ثانیه)
- حجم فایل
- تاریخ آپلود

**کاربرد**: بارگذاری و مدیریت فایل‌های صوتی/تصویری برنامه

---

## بخش 4: ویرایش و تاریخچه

### 4.1 ProgramVersion (نسخه‌های برنامه)
- نمونهٔ برنامه
- شماره نسخه
- توضیحات تغییرات
- تاریخ ایجاد
- ایجاد‌شده توسط

**کاربرد**: ثبت نسخه‌های مختلف و تاریخچهٔ تغییرات هر برنامه

---

### 4.2 ScheduleModification (تعدیلات برنامه‌ریزی)
- نمونهٔ برنامه
- نوع تعدیل (تغییر زمان/لغو/تعویق/جایگزینی)
- دلیل
- زمان شروع/پایان قبلی
- زمان شروع/پایان جدید
- تاریخ تعدیل
- تعدیل‌شده توسط

**کاربرد**: ثبت تمام تغییرات برنامه‌ریزی (با حفظ سابقهٔ کامل)

---

## بخش 5: مدیریت و کنترل (کنداکتور)

### 5.1 Schedule (برنامه‌ریزی نهایی)
- شبکه
- تاریخ برنامه‌ریزی
- نمونهٔ برنامه (OneToOne)
- ترتیب پخش در روز
- فعال/غیرفعال
- یادداشت‌های کنداکتور

**کاربرد**: تعریف برنامه‌ریزی نهایی برای هر روز

**نکتهٔ مهم**: هر نمونهٔ برنامه دقیقاً یک Schedule دارد

---

### 5.2 Conductor (کنداکتور - دستورالعمل‌های خودکار)
- Schedule (OneToOneField)

**دستورالعمل‌های خودکار:**
- پخش خودکار اپنینگ
- پخش خودکار انگلیزی
- تنظیم خودکار صدا
- ضبط خودکار

**اطلاعات اضافی:**
- اولویت (1-5: خیلی کم تا خیلی زیاد)
- برنامه جایگزین (در صورت مشکل)
- دستورالعمل‌های خاص
- شخص تماس
- وضعیت (فعال/غیرفعال)

**کاربرد**: تعریف دستورالعمل‌های اتوماتیک برای هر برنامه

---

### 5.3 ConductorLog (ثبت‌های کنداکتور)
- کنداکتور
- اقدام (شروع/پخش اپنینگ/پخش برنامه/پخش انگلیزی/توقف/ادامه/پایان/خطا/مداخلهٔ دستی)
- وضعیت (موفق/ناموفق/هشدار)
- پیام
- زمان

**کاربرد**: ثبت کامل تمام اقدامات کنداکتور برای پیگیری و خطایابی

---

## بخش 6: تحلیل و آمار

### 6.1 AnalyticsData (داده‌های تحلیلی)
- نمونهٔ برنامه (OneToOne)
- تعداد کل بینندگان
- امتیاز متوسط (0-10)
- تعداد نظرات
- ذکر در شبکه‌های اجتماعی

**کاربرد**: ثبت آمار و تحلیل عملکرد هر برنامه

---

## نمودار ارتباطات

```
Channel (شبکه)
    ├── Program (برنامه)
    │   ├── ProgramMember (اعضای برنامه)
    │   │   ├── Person
    │   │   └── Role
    │   ├── ProgramInstance (نمونهٔ برنامه)
    │   │   ├── ProgramGuest (مهمانان)
    │   │   ├── ProgramAsset (فایل‌ها)
    │   │   ├── ProgramVersion (نسخه‌ها)
    │   │   ├── ScheduleModification (تعدیلات)
    │   │   ├── AnalyticsData (آمار)
    │   │   └── Schedule (برنامه‌ریزی)
    │   │       └── Conductor (کنداکتور)
    │   │           └── ConductorLog (ثبت‌ها)
    │   └── Genre (ژانرها)
    └── TargetAudience (مخاطبین هدف)
```

---

## جریان استفاده

### مرحلهٔ 1: تعریف داده‌های مرجع
1. تعریف شبکه‌ها (Channels)
2. تعریف ژانرها (Genres)
3. تعریف نقش‌ها (Roles)
4. ثبت افراد (Persons)
5. تعریف مخاطبین هدف (TargetAudiences)

### مرحلهٔ 2: ورود برنامه‌ها
1. ایجاد برنامهٔ جدید (Program)
2. اختصاص اعضای برنامه (ProgramMembers)
3. اختصاص ژانرها و مخاطبین هدف

### مرحلهٔ 3: برنامه‌ریزی و ورود اطلاعات برای پخش
1. ایجاد نمونهٔ برنامه (ProgramInstance)
2. اضافه کردن مهمانان (ProgramGuests)
3. بارگذاری فایل‌ها (ProgramAssets)
4. ایجاد Schedule و تعریف کنداکتور

### مرحلهٔ 4: پخش و نظارت
1. کنداکتور اقدامات را خودکار انجام می‌دهد
2. ثبت‌های کنداکتور ذخیره می‌شوند
3. داده‌های آمار جمع‌آوری می‌شوند

### مرحلهٔ 5: تحلیل و ارزیابی
1. بررسی آمار (AnalyticsData)
2. بررسی ثبت‌های کنداکتور
3. تحلیل موفقیت برنامه

---

## نکات مهم

1. **حفاظت از اطلاعات**: تمام تغییرات ثبت می‌شوند (ProgramVersion, ScheduleModification, ConductorLog)

2. **اتوماسیون**: کنداکتور اقدامات را طبق دستورالعمل خودکار انجام می‌دهد

3. **انعطاف‌پذیری**: امکان تعریف دستورالعمل‌های خاص برای هر برنامه

4. **جایگزین‌سازی**: امکان تعریف برنامهٔ جایگزین در صورت مشکل

5. **پیگیری**: تمام اقدامات و نتایج ثبت می‌شوند

---

## توسعه‌های آینده

1. رابط کاربری وب برای مدیریت کنندگان
2. رابط کاربری برای مجریان و تهیه‌کنندگان
3. API برای ارسال اطلاعات به سیستم‌های دیگر
4. سیستم اطلاع‌رسانی خودکار
5. داشبورد تحلیلی پیشرفته
6. ادغام با سیستم‌های دیگر (ویرایشگر، پخش‌کننده، ...)
